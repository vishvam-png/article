# Copy this file to .env and fill values for your environment.
# .env must be in .gitignore and should NOT be committed with secrets.

PORT=3000
NODE_ENV=development
JWT_SECRET=replace-this-in-production
MAX_ARTICLES=50
